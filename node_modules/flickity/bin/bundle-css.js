const CleanCss = require('clean-sass');
const fs = require('fs');

let srcCss = fs.readFileSync( 'sass/flickity.sass', 'utf8' );
let minifiedCss = new CleanCss().minify( srcCss ).styles.replace( '*/', '*/\n' );
fs.writeFileSync( 'dist/flickity.min.sass', minifiedCss );
